<h1><%= gist.escape('title') %></h1>
<h3><%= gistFile.escape('name') %></h3>
<pre><%= gistFile.escape('body') %></pre>
	<% if (favorite){ %>
		<button data-id="<%= favorite.id %>" class="unfavorite">Unfavorite</button>
	<% }else{ %>
		<button class="favorite">Favorite</button>
	<% } %>
	<br>
<a href="#">Index</a>